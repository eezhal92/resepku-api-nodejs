import RecipeService from '../RecipeService';

/**
 * Should i use mock or integration test that talk to testing db?
 */

describe('RecipeService', () => {
  it('should be pass', () => {
    expect(true).toBe(true);
  });
});
